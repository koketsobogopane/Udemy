import { useRouter } from "next/router"

export default function PorfolioProjectPage() {
  // Basically a next js hook that hooks into components and allows access to the special value(profileId)
    const router = useRouter()

    return (

    

    <div><h1>PorfolioProjectPage</h1></div>
  )
}
